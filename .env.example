# YShvydak Test Dashboard - Core Configuration
# Only 5 variables needed - all others are derived automatically

# Server Configuration
PORT=3001
NODE_ENV=development

# Test Project Integration
PLAYWRIGHT_PROJECT_DIR=/path/to/your/playwright/project

# Reporter Configuration
USE_NPM_REPORTER=false

# Base URL Configuration (single source of truth for all services)
VITE_PORT=3000
BASE_URL=http://localhost:3001
VITE_BASE_URL=http://localhost:3001

# All other variables are derived automatically:
# - DASHBOARD_API_URL = BASE_URL (server API)
# - VITE_API_BASE_URL = BASE_URL/api (web API)  
# - VITE_WEBSOCKET_URL = ws://BASE_URL/ws (WebSocket)
# - VITE_SERVER_URL = BASE_URL (web server)
# - OUTPUT_DIR = test-results (default storage)